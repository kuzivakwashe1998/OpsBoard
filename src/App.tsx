/**
 * Kept as a re-export for tooling familiarity — routing lives in router.tsx
 * and the shell layout lives in components/layout/AppShell.tsx.
 */
export { AppShell as App } from './components/layout/AppShell'
